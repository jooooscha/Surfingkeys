module.exports = function(api) {
    const {
        addSearchAlias,
        cmap,
        getBrowserName,
        getFormData,
        map,
        mapkey,
        readText,
        tabOpenLink,
        vmapkey,
        Clipboard,
        Hints,
        Front,
        RUNTIME
    } = api;
}
